// Generated by CoffeeScript 1.3.3
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  FitgemClient.Routers.ResourcesRouter = (function(_super) {

    __extends(ResourcesRouter, _super);

    function ResourcesRouter() {
      return ResourcesRouter.__super__.constructor.apply(this, arguments);
    }

    ResourcesRouter.prototype.routes = {
      "/": "show"
    };

    ResourcesRouter.prototype.initialize = function(options) {
      Backbone.emulateHTTP = true;
      this.user = new FitgemClient.Models.User(options);
      return this.body_measurements = new FitgemClient.Models.BodyMeasurements();
    };

    ResourcesRouter.prototype.show = function() {
      var view,
        _this = this;
      console.log('test');
      if (this.user.get("linked")) {
        if ($('#fitbit-user-data')) {
          console.log('test');
          this.user.fetch({
            success: function() {
              var user_view;
              user_view = new FitgemClient.Views.Resources.ShowUserView({
                model: _this.user
              });
              return $('#fitbit-user-data').html(user_view.render().el);
            },
            error: function() {
              var view;
              view = new FitgemClient.Views.Common.ConnectionErrorView();
              return $(".fitbit-data-view").html(view.render().el);
            }
          });
        }
        if ($('#fitbit-body-measurements-data')) {
          console.log('123test');
          return this.body_measurements.fetch({
            success: function() {
              var user_view;
              user_view = new FitgemClient.Views.Resources.ShowBodyMeasurementsView({
                model: _this.body_measurements
              });
              return $('#fitbit-body-measurements-data').html(user_view.render().el);
            },
            error: function() {
              var view;
              view = new FitgemClient.Views.Common.ConnectionErrorView();
              return $(".fitbit-data-view").html(view.render().el);
            }
          });
        }
      } else if (this.user.get("logged_in")) {
        view = new FitgemClient.Views.Common.AccountsNotLinkedView();
        $(".fitbit-data-view").html(view.render().el);
        return console.log('test');
      } else {
        view = new FitgemClient.Views.Common.NotLoggedInView();
        $(".fitbit-data-view").html(view.render().el);
        return console.log('test');
      }
    };

    return ResourcesRouter;

  })(Backbone.Router);

}).call(this);
